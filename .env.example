# B.O.B Environment Configuration
# Copy to .env and customize

# =============================================================================
# Database
# =============================================================================
# Path to SQLite database (relative or absolute)
# BOB_DB_PATH=./data/bob.db

# =============================================================================
# Embedding Model
# =============================================================================
# Model name from sentence-transformers
# Common options:
#   - all-MiniLM-L6-v2 (fast, 384 dims, default)
#   - all-mpnet-base-v2 (better quality, 768 dims)
#   - multi-qa-MiniLM-L6-cos-v1 (optimized for Q&A)
# BOB_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Device for embedding (cpu, cuda, mps)
# BOB_EMBEDDING_DEVICE=cpu

# =============================================================================
# Defaults
# =============================================================================
# Default project name when not specified
# BOB_DEFAULT_PROJECT=main

# Default language for documents (ISO 639-1)
# BOB_DEFAULT_LANGUAGE=en

# Default number of results to return
# BOB_DEFAULT_TOP_K=5

# =============================================================================
# Date Confidence
# =============================================================================
# Days threshold for HIGH confidence (default: 30)
# BOB_DATE_HIGH_MAX_DAYS=30

# Days threshold for MEDIUM confidence (default: 90)
# BOB_DATE_MEDIUM_MAX_DAYS=90

# Days threshold for outdated warning (default: 180)
# BOB_DATE_OUTDATED_THRESHOLD_DAYS=180

# =============================================================================
# Optional: Local LLM (Phase 4)
# =============================================================================
# Path to GGUF model file for local generation
# BOB_LLM_MODEL_PATH=./data/models/llama-2-7b-chat.Q4_K_M.gguf

# Context size for LLM
# BOB_LLM_CONTEXT_SIZE=4096

# Temperature for generation (0.0 = deterministic)
# BOB_LLM_TEMPERATURE=0.0

# =============================================================================
# Logging
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR
# BOB_LOG_LEVEL=INFO

# =============================================================================
# Notes
# =============================================================================
# - All BOB_* environment variables override config file settings
# - The /data directory is gitignored - keep personal data there
# - No API keys are required for core functionality
# - This is a local-first tool - no cloud services needed
